import Foundation

/// Предоставляет текущее календарное время для расчёта абсолютных границ.
protocol DateProviding: Sendable {
    /// Возвращает текущую дату; тесты могут управлять результатом детерминированно.
    func now() -> Date
}
